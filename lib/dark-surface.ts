/**
 * Text colors for panels on `bg-stone-900` (and similar dark greys).
 * Muted copy matches the dark `SiteFooter` (`text-stone-300`).
 */
export const DARK_SURFACE_MUTED_TEXT = "text-stone-300";
export const DARK_SURFACE_HEADING_TEXT = "text-white";

/** Inline link on a dark panel (footer-style: muted, brightens on hover). */
export const DARK_SURFACE_LINK_CLASS =
  "border-b border-stone-500 text-stone-300 transition-colors hover:border-white hover:text-white";
